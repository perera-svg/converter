# JSON ↔ INI Conversion Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Implement JSON→INI and INI→JSON converters using the `ini` npm package, activating the two "Coming Soon" cards already present in the hub grid.

**Architecture:** Two new converter files follow the established pattern (`convert(input, options): string`), wired into the central dispatcher in `index.ts`. Validation guards are applied before calling the library to enforce INI's structural constraints (no arrays, max one level of nesting).

**Tech Stack:** `ini` npm package, TypeScript, Vitest for tests, pnpm as package manager.

---

### Task 1: Install the `ini` package

**Files:**
- Modify: `package.json` (pnpm adds automatically)
- Modify: `pnpm-lock.yaml` (pnpm updates automatically)

**Step 1: Install the package**

```bash
pnpm add ini
pnpm add -D @types/ini
```

**Step 2: Verify installation**

```bash
pnpm list ini @types/ini
```

Expected output includes `ini` and `@types/ini` in the list.

**Step 3: Commit**

```bash
git add package.json pnpm-lock.yaml
git commit -m "chore: add ini package for JSON↔INI conversion"
```

---

### Task 2: Write failing tests for `json-ini.ts`

**Files:**
- Create: `app/_lib/converters/__tests__/json-ini.test.ts`

**Step 1: Create the test file**

```typescript
import { describe, expect, it } from "vitest";
import { convert } from "../json-ini";

const opts = { indent: 2 };

describe("json-ini", () => {
  it("converts a flat object to key=value pairs", () => {
    const result = convert(JSON.stringify({ name: "app", version: "1.0" }), opts);
    expect(result).toContain("name");
    expect(result).toContain("app");
    expect(result).toContain("version");
    expect(result).toContain("1.0");
  });

  it("converts a one-level nested object to a [section] block", () => {
    const result = convert(JSON.stringify({ db: { host: "localhost", port: 3306 } }), opts);
    expect(result).toContain("[db]");
    expect(result).toContain("host");
    expect(result).toContain("localhost");
    expect(result).toContain("port");
    expect(result).toContain("3306");
  });

  it("handles mixed top-level scalars and sections", () => {
    const result = convert(JSON.stringify({ name: "app", db: { host: "localhost" } }), opts);
    expect(result).toContain("name");
    expect(result).toContain("app");
    expect(result).toContain("[db]");
    expect(result).toContain("host");
  });

  it("throws on empty input", () => {
    expect(() => convert("", opts)).toThrow("Input is empty");
  });

  it("throws on whitespace-only input", () => {
    expect(() => convert("   ", opts)).toThrow("Input is empty");
  });

  it("throws on top-level array", () => {
    expect(() => convert(JSON.stringify([1, 2]), opts)).toThrow(
      "INI requires an object at the top level",
    );
  });

  it("throws on top-level null", () => {
    expect(() => convert(JSON.stringify(null), opts)).toThrow(
      "INI requires an object at the top level",
    );
  });

  it("throws on top-level scalar", () => {
    expect(() => convert(JSON.stringify(42), opts)).toThrow(
      "INI requires an object at the top level",
    );
  });

  it("throws on array as a value", () => {
    expect(() => convert(JSON.stringify({ tags: ["a", "b"] }), opts)).toThrow(
      'INI does not support arrays — found at key "tags"',
    );
  });

  it("throws on array inside a section", () => {
    expect(() => convert(JSON.stringify({ db: { hosts: ["a", "b"] } }), opts)).toThrow(
      'INI does not support arrays — found at key "db.hosts"',
    );
  });

  it("throws on object nested more than one level deep", () => {
    expect(() =>
      convert(JSON.stringify({ config: { database: { host: "localhost" } } }), opts),
    ).toThrow('INI supports one level of nesting — found nested object at "config.database"');
  });

  it("throws SyntaxError on invalid JSON", () => {
    expect(() => convert("{ bad json }", opts)).toThrow(SyntaxError);
  });
});
```

**Step 2: Run tests to confirm they fail**

```bash
pnpm test -- json-ini
```

Expected: FAIL with "Cannot find module '../json-ini'"

**Step 3: Commit**

```bash
git add app/_lib/converters/__tests__/json-ini.test.ts
git commit -m "test: add failing tests for json-ini converter"
```

---

### Task 3: Implement `json-ini.ts`

**Files:**
- Create: `app/_lib/converters/json-ini.ts`

**Step 1: Create the converter**

```typescript
import { stringify as iniStringify } from "ini";
import type { ConvertOptions } from "./types";

export function convert(input: string, _options: ConvertOptions): string {
  if (!input.trim()) throw new Error("Input is empty");
  const parsed = JSON.parse(input);
  if (parsed === null || typeof parsed !== "object" || Array.isArray(parsed)) {
    throw new Error("INI requires an object at the top level");
  }
  for (const [key, value] of Object.entries(parsed as Record<string, unknown>)) {
    if (Array.isArray(value)) {
      throw new Error(`INI does not support arrays — found at key "${key}"`);
    }
    if (value !== null && typeof value === "object") {
      for (const [subKey, subValue] of Object.entries(value as Record<string, unknown>)) {
        if (Array.isArray(subValue)) {
          throw new Error(`INI does not support arrays — found at key "${key}.${subKey}"`);
        }
        if (subValue !== null && typeof subValue === "object") {
          throw new Error(
            `INI supports one level of nesting — found nested object at "${key}.${subKey}"`,
          );
        }
      }
    }
  }
  return iniStringify(parsed as Record<string, unknown>);
}
```

**Step 2: Run tests**

```bash
pnpm test -- json-ini
```

Expected: All tests PASS.

**Step 3: Commit**

```bash
git add app/_lib/converters/json-ini.ts
git commit -m "feat: implement json-ini converter"
```

---

### Task 4: Write failing tests for `ini-json.ts`

**Files:**
- Create: `app/_lib/converters/__tests__/ini-json.test.ts`

**Step 1: Create the test file**

```typescript
import { describe, expect, it } from "vitest";
import { convert } from "../ini-json";

const opts = { indent: 2 };

describe("ini-json", () => {
  it("converts flat key=value pairs to a JSON object", () => {
    const input = "name=app\nversion=1.0";
    const result = JSON.parse(convert(input, opts));
    expect(result).toMatchObject({ name: "app", version: "1.0" });
  });

  it("converts sectioned INI to a nested JSON object", () => {
    const input = "[db]\nhost=localhost\nport=3306";
    const result = JSON.parse(convert(input, opts));
    expect(result).toMatchObject({ db: { host: "localhost", port: "3306" } });
  });

  it("respects the indent option — 2 spaces", () => {
    const input = "name=app";
    const result = convert(input, { indent: 2 });
    expect(result).toMatch(/^\{\n {2}/);
  });

  it("respects the indent option — 4 spaces", () => {
    const input = "name=app";
    const result = convert(input, { indent: 4 });
    expect(result).toMatch(/^\{\n {4}/);
  });

  it("throws on empty input", () => {
    expect(() => convert("", opts)).toThrow("Input is empty");
  });

  it("throws on whitespace-only input", () => {
    expect(() => convert("   ", opts)).toThrow("Input is empty");
  });
});
```

**Step 2: Run tests to confirm they fail**

```bash
pnpm test -- ini-json
```

Expected: FAIL with "Cannot find module '../ini-json'"

**Step 3: Commit**

```bash
git add app/_lib/converters/__tests__/ini-json.test.ts
git commit -m "test: add failing tests for ini-json converter"
```

---

### Task 5: Implement `ini-json.ts`

**Files:**
- Create: `app/_lib/converters/ini-json.ts`

**Step 1: Create the converter**

```typescript
import { parse as iniParse } from "ini";
import type { ConvertOptions } from "./types";

export function convert(input: string, options: ConvertOptions): string {
  if (!input.trim()) throw new Error("Input is empty");
  const parsed = iniParse(input);
  const indent = options.indent === "\t" ? "\t" : Number(options.indent);
  return JSON.stringify(parsed, null, indent);
}
```

**Step 2: Run tests**

```bash
pnpm test -- ini-json
```

Expected: All tests PASS.

**Step 3: Commit**

```bash
git add app/_lib/converters/ini-json.ts
git commit -m "feat: implement ini-json converter"
```

---

### Task 6: Wire up converters in `index.ts` and activate pairs in `formats.ts`

**Files:**
- Modify: `app/_lib/converters/index.ts`
- Modify: `app/_lib/formats.ts`

**Step 1: Update `index.ts`**

Add these two import lines alongside the existing ones (alphabetical order fits after `ini-json` / `json-ini`):

```typescript
import { convert as iniToJson } from "./ini-json";
import { convert as jsonToIni } from "./json-ini";
```

Add these two entries to the `converters` map:

```typescript
"JSON→INI": jsonToIni,
"INI→JSON": iniToJson,
```

The full updated file should look like:

```typescript
import type { Format } from "../formats";
import type { ConvertOptions } from "./types";
import { convert as csvToJson } from "./csv-json";
import { convert as iniToJson } from "./ini-json";
import { convert as jsonToCsv } from "./json-csv";
import { convert as jsonToIni } from "./json-ini";
import { convert as jsonToToml } from "./json-toml";
import { convert as jsonToXml } from "./json-xml";
import { convert as jsonToYaml } from "./json-yaml";
import { convert as tomlToJson } from "./toml-json";
import { convert as xmlToJson } from "./xml-json";
import { convert as yamlToJson } from "./yaml-json";

type ConverterFn = (input: string, options: ConvertOptions) => string;

const converters: Partial<Record<string, ConverterFn>> = {
  "JSON→YAML": jsonToYaml,
  "YAML→JSON": yamlToJson,
  "JSON→TOML": jsonToToml,
  "TOML→JSON": tomlToJson,
  "JSON→XML": jsonToXml,
  "XML→JSON": xmlToJson,
  "JSON→CSV": jsonToCsv,
  "CSV→JSON": csvToJson,
  "JSON→INI": jsonToIni,
  "INI→JSON": iniToJson,
};

export function dispatch(
  from: Format,
  to: Format,
  input: string,
  options: ConvertOptions,
): string {
  const key = `${from}→${to}`;
  const fn = converters[key];
  if (!fn) throw new Error(`Unsupported conversion: ${key}`);
  return fn(input, options);
}
```

**Step 2: Update `formats.ts`**

Add `"JSON→INI"` and `"INI→JSON"` to `IMPLEMENTED_PAIRS` in `app/_lib/formats.ts:135-144`:

```typescript
export const IMPLEMENTED_PAIRS = new Set<PairKey>([
  "JSON→YAML",
  "YAML→JSON",
  "JSON→TOML",
  "TOML→JSON",
  "JSON→XML",
  "XML→JSON",
  "JSON→CSV",
  "CSV→JSON",
  "JSON→INI",
  "INI→JSON",
]);
```

**Step 3: Run the full test suite**

```bash
pnpm test
```

Expected: All tests PASS.

**Step 4: Commit**

```bash
git add app/_lib/converters/index.ts app/_lib/formats.ts
git commit -m "feat: wire JSON↔INI converters and activate hub cards"
```

---

### Task 7: Verify in browser

**Step 1: Start the dev server**

```bash
pnpm dev
```

**Step 2: Check these URLs**

- `http://localhost:3000` — hub grid should show JSON→INI and INI→JSON as **active** (no "Soon" badge)
- `http://localhost:3000/json/ini` — paste `{"name":"app","db":{"host":"localhost"}}`, verify INI output
- `http://localhost:3000/ini/json` — paste `[db]\nhost=localhost`, verify JSON output
- `http://localhost:3000/json/ini` — paste `{"tags":["a","b"]}`, verify error message appears in the status strip
- `http://localhost:3000/json/ini` — paste `{"a":{"b":{"c":1}}}`, verify nesting error appears

**Step 3: Stop the dev server** (`Ctrl+C`)
